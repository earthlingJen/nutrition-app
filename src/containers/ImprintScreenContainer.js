import { connect } from 'react-redux'
import ImprintScreen from '../screens/ImprintScreen'

// const mapStateToProps = state => ({

// })

const mapDispatchToProps = () => ({})

export default connect(mapDispatchToProps)(ImprintScreen)
